# Dev server host/host header controls
# Set to true to disable host checking in dev (recommended for ephemeral hosts like Replit/Lovable)
ALLOW_ALL_HOSTS=
# Or list specific allowed hosts (comma-separated), e.g.:
# ALLOWED_HOSTS=your-host-1.example.com,your-host-2.example.com
