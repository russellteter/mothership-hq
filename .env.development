# Allows any Host header in Vite dev server (development only)
ALLOW_ALL_HOSTS=true
# Alternatively, specify exact hosts if you want to stay strict (comma-separated)
# ALLOWED_HOSTS=1288bbe6-....spock.replit.dev
